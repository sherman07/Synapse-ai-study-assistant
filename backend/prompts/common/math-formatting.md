# Math Formatting

- Keep mathematical notation MathJax-compatible.
- Use inline formulas with `\( ... \)`.
- Use display equations with `\[ ... \]`.
- Use matrix environments such as `bmatrix` or `pmatrix`; do not output raw matrix arrays like `[[1,2],[3,4]]`.
- Explain what each variable or symbol means when it matters for learning.

